# Clerk publishable key (safe to ship to the browser). Required.
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxx

# Optional: override the dev API proxy target (defaults to http://127.0.0.1:8765).
# VITE_API_PROXY=http://127.0.0.1:8765
