# Build-time config for the agent_team plugin build (`vite build --mode agent-team`).
# The plugin serves this SPA at /agent-team and authenticates via the host's
# session cookie, so no Clerk key is needed.
VITE_API_BASE=/api/agent-team
VITE_AUTH_MODE=session
VITE_ROUTER_BASE=/agent-team
